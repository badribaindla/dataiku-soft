{
    "meta" : {
        "label" : "Install R package",
        "description" : "Installs 'dataiku' R package for user",
        "category" : "R dev setup on DSS server",
        "icon" : "icon-code"
    },

    "impersonate" : true,

    "permissions" : ["WRITE_CONF"],

    "macroRoles": [
        {"type": "RSTUDIO_SERVER_EMBED"}
    ],

    "params": [
    ]
}
