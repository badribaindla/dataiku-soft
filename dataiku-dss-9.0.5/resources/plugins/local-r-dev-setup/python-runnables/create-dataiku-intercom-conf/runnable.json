{
    "meta" : {
        "label" : "Setup connectivity to DSS",
        "description" : "Sets up connectivity to the current DSS instance",
        "category" : "R dev setup on DSS server",
        "icon" : "icon-code"
    },

    "impersonate" : true,

    "permissions" : ["WRITE_CONF"],

    "macroRoles": [
        {"type": "RSTUDIO_SERVER_EMBED"}
    ],

    "params": [
        {
            "name":"forceIfConfigExists",
            "label": "Even if conf. file exists",
            "description": "If enabled and configuration file already exists, it will be overwritten. If disabled and configuration file already exists, it will fail",
            "type": "BOOLEAN"
        }
    ]
}
